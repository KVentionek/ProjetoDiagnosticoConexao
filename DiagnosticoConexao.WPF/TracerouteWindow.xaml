<Window x:Class="DiagnosticoConexao.WPF.TracerouteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Traceroute"
        Height="700"
        Width="1100"
        MinHeight="600"
        MinWidth="900"
        Background="#0F1116"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">

    <Window.Resources>

        <!-- CORES -->

        <SolidColorBrush x:Key="CardBrush" Color="#191C23"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#272B35"/>

        <SolidColorBrush x:Key="PrimaryBrush" Color="#E05A9D"/>
        <SolidColorBrush x:Key="PrimaryHoverBrush" Color="#EB70AD"/>
        <SolidColorBrush x:Key="PrimaryPressedBrush" Color="#C94788"/>

        <SolidColorBrush x:Key="TextBrush" Color="#F5F5F7"/>
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="#9297A3"/>

        <!-- SOMBRAS -->

        <DropShadowEffect x:Key="CardShadow"
                          Color="#000000"
                          BlurRadius="18"
                          ShadowDepth="4"
                          Opacity="0.30"/>

        <DropShadowEffect x:Key="ButtonShadow"
                          Color="#000000"
                          BlurRadius="12"
                          ShadowDepth="3"
                          Opacity="0.35"/>


        <!-- BOTÃO PRINCIPAL -->

        <Style x:Key="ModernButton"
               TargetType="{x:Type Button}">

            <Setter Property="Background"
                    Value="{StaticResource PrimaryBrush}"/>

            <Setter Property="Foreground"
                    Value="White"/>

            <Setter Property="BorderThickness"
                    Value="0"/>

            <Setter Property="FontSize"
                    Value="13"/>

            <Setter Property="FontWeight"
                    Value="SemiBold"/>

            <Setter Property="Cursor"
                    Value="Hand"/>

            <Setter Property="Padding"
                    Value="18,0"/>

            <Setter Property="Effect"
                    Value="{StaticResource ButtonShadow}"/>

            <Setter Property="Template">

                <Setter.Value>

                    <ControlTemplate TargetType="{x:Type Button}">

                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="9"
                                Padding="{TemplateBinding Padding}">

                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                RecognizesAccessKey="True"/>

                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver"
                                     Value="True">

                                <Setter TargetName="ButtonBorder"
                                        Property="Background"
                                        Value="{StaticResource PrimaryHoverBrush}"/>

                            </Trigger>

                            <Trigger Property="IsPressed"
                                     Value="True">

                                <Setter TargetName="ButtonBorder"
                                        Property="Background"
                                        Value="{StaticResource PrimaryPressedBrush}"/>

                            </Trigger>

                            <Trigger Property="IsEnabled"
                                     Value="False">

                                <Setter TargetName="ButtonBorder"
                                        Property="Opacity"
                                        Value="0.45"/>

                            </Trigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>

                </Setter.Value>

            </Setter>

        </Style>


        <!-- CARD -->

        <Style x:Key="CardStyle"
               TargetType="{x:Type Border}">

            <Setter Property="Background"
                    Value="{StaticResource CardBrush}"/>

            <Setter Property="CornerRadius"
                    Value="14"/>

            <Setter Property="Effect"
                    Value="{StaticResource CardShadow}"/>

        </Style>


        <!-- TEXTBOX -->

        <Style x:Key="ModernTextBox"
               TargetType="{x:Type TextBox}">

            <Setter Property="Background"
                    Value="#15181E"/>

            <Setter Property="Foreground"
                    Value="{StaticResource TextBrush}"/>

            <Setter Property="BorderBrush"
                    Value="{StaticResource BorderBrush}"/>

            <Setter Property="BorderThickness"
                    Value="1"/>

            <Setter Property="FontSize"
                    Value="14"/>

            <Setter Property="Padding"
                    Value="14,0"/>

            <Setter Property="Height"
                    Value="46"/>

            <Setter Property="VerticalContentAlignment"
                    Value="Center"/>

        </Style>

    </Window.Resources>


    <!-- CONTEÚDO -->

    <Grid Margin="30">

        <Grid.RowDefinitions>

            <RowDefinition Height="Auto"/>

            <RowDefinition Height="24"/>

            <RowDefinition Height="Auto"/>

            <RowDefinition Height="20"/>

            <RowDefinition Height="*"/>

        </Grid.RowDefinitions>


        <!-- CABEÇALHO -->

        <Grid Grid.Row="0">

            <StackPanel>

                <TextBlock
                    Text="TRACEROUTE"
                    Foreground="{StaticResource TextBrush}"
                    FontSize="30"
                    FontWeight="Bold"/>

                <TextBlock
                    Text="Analise o caminho percorrido até um destino."
                    Foreground="{StaticResource SecondaryTextBrush}"
                    FontSize="14"
                    Margin="0,7,0,0"/>

            </StackPanel>

        </Grid>


        <!-- DESTINO -->

        <Border Grid.Row="2"
                Style="{StaticResource CardStyle}"
                Padding="20">

            <Grid>

                <Grid.ColumnDefinitions>

                    <ColumnDefinition Width="*"/>

                    <ColumnDefinition Width="15"/>

                    <ColumnDefinition Width="150"/>

                </Grid.ColumnDefinitions>


                <StackPanel Grid.Column="0">

                    <TextBlock
                        Text="DESTINO"
                        Foreground="{StaticResource SecondaryTextBrush}"
                        FontSize="11"
                        FontWeight="SemiBold"
                        Margin="2,0,0,7"/>

                    <TextBox
                        x:Name="TxtDestination"
                        Style="{StaticResource ModernTextBox}"
                        Text="google.com"/>

                </StackPanel>


                <Button
                    x:Name="BtnTrace"
                    Grid.Column="2"
                    Content="Executar traceroute"
                    Height="46"
                    VerticalAlignment="Bottom"
                    Style="{StaticResource ModernButton}"
                    Click="ExecutarTraceroute_Click"/>

            </Grid>

        </Border>


        <!-- RESULTADO -->

        <Border Grid.Row="4"
                Style="{StaticResource CardStyle}"
                Padding="24">

            <Grid>

                <Grid.RowDefinitions>

                    <RowDefinition Height="Auto"/>

                    <RowDefinition Height="15"/>

                    <RowDefinition Height="*"/>

                </Grid.RowDefinitions>


                <Grid Grid.Row="0">

                    <Grid.ColumnDefinitions>

                        <ColumnDefinition Width="*"/>

                        <ColumnDefinition Width="Auto"/>

                    </Grid.ColumnDefinitions>


                    <StackPanel>

                        <TextBlock
                            Text="RESULTADO"
                            Foreground="{StaticResource TextBrush}"
                            FontSize="16"
                            FontWeight="Bold"/>

                        <TextBlock
                            Text="Saltos identificados até o destino."
                            Foreground="{StaticResource SecondaryTextBrush}"
                            FontSize="12"
                            Margin="0,5,0,0"/>

                    </StackPanel>

                </Grid>


                <Border
                    Grid.Row="2"
                    Background="#15181E"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="9"
                    Padding="18">

                    <ScrollViewer
                        VerticalScrollBarVisibility="Auto"
                        HorizontalScrollBarVisibility="Auto">

                        <TextBlock
                            x:Name="TxtTraceResult"
                            Text="Informe um destino e execute o traceroute."
                            Foreground="#D5D7DC"
                            FontFamily="Consolas"
                            FontSize="14"
                            TextWrapping="NoWrap"/>

                    </ScrollViewer>

                </Border>

            </Grid>

        </Border>

    </Grid>

</Window>